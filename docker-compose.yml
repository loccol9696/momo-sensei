services:
#   mssql:
#     image: mcr.microsoft.com/mssql/server:2022-latest
#     container_name: momo-mssql
#     user: "0:0"
#     environment:
#       - ACCEPT_EULA=Y
#       - MSSQL_SA_PASSWORD=sa_Password12345
#     ports:
#       - "1433:1433"
#     volumes:
#       - ./sql_data:/var/opt/mssql/data
#     healthcheck:
#       test: [ "CMD", "/opt/mssql-tools18/bin/sqlcmd", "-S", "localhost", "-U", "sa", "-P", "sa_Password12345", "-C", "-Q", "SELECT 1" ]
#       interval: 10s
#       timeout: 3s
#       retries: 10
# 
#   mssql-init:
#     image: mcr.microsoft.com/mssql/server:2022-latest
#     container_name: momo-mssql-init
#     depends_on:
#       mssql:
#         condition: service_healthy
#     entrypoint: >
#       /bin/bash -c "/opt/mssql-tools18/bin/sqlcmd -S mssql -U sa -P sa_Password12345 -C -Q \"IF NOT EXISTS (SELECT name FROM sys.databases WHERE name = 'momo-sensei') CREATE DATABASE [momo-sensei]\""

  redis:
    image: redis:alpine
    container_name: momo-redis
    ports:
      - "6379:6379"

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: momo-backend
    ports:
      - "8080:8080"
    depends_on:
      redis:
        condition: service_started
    env_file:
      - .env
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
